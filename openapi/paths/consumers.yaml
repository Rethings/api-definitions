parameters:
  - $ref: ../components/parameters/header/appId.yaml
post:
  tags:
    - Consumers
  summary: Register a Consumer
  operationId: RegisterConsumer
  description: |
    Register a consumer
  requestBody:
    $ref: ../components/requests/Consumer.yaml
  responses:
    '201':
      description: A consumer was registered successfully
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Consumer.yaml
