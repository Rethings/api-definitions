parameters:
  - $ref: ../components/parameters/header/appId.yaml
  - $ref: ../components/parameters/path/consumerId.yaml
get:
  tags:
    - Consumers
  summary: Retrieve a Consumer's MQTT Credentials
  description: |
    Retrieve a consumer's MQTT credentials.
  operationId: GetConsumerCredentials
  responses:
    '200':
      description: A consumer's MQTT credentials was retrieved successfully
      content:
        application/json:
          schema:
            $ref: ../components/schemas/MQTTCredentials.yaml
